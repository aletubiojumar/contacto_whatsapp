- Cada función debe incluir un bloque de comentario:
   * Type hints en la firma
   * Docstrings estilo Google
   * Sphinx o MkDocs para documentación
- Todas las variables y funciones deben escribirse en snake_case.
- No uses camelCase.
- En el README tiene que haber un esquema de la estructura del proyecto indicando que hace cada archivo a rasgos generales:
- Los comentarios y docstrings deben estar en español con acentos validos (UTF-8).
- Permitir solo caracteres Unicode usados en español (áéíóúüñ¿¡).
- Evitar caracteres mojibake o raros (ej: �, Ã, Â, И, ツ). Si aparecen, corregirlos.